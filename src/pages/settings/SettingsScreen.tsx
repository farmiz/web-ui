import DashboardLayout from '@/components/dashboard/Layout'

const SettingsScreen = () => {
  return (
    <DashboardLayout pageTitle="Settings" showSidebar={false}>
    <p>Settings</p>
   </DashboardLayout>
  )
}

export default SettingsScreen